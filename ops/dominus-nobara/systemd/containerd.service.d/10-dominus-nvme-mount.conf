[Unit]
# Docker 29 stores image content in containerd separately from DockerRootDir.
# Gate containerd itself on the RAID so neither store spills onto root.
RequiresMountsFor=/mnt/nvmer0
After=mnt-nvmer0.mount

[Service]
ExecStartPre=/usr/bin/mountpoint --quiet /mnt/nvmer0
