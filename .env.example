# Required: must match aichat's POSTGRES_PASSWORD
POSTGRES_PASSWORD=

# Optional: Anthropic API key for Claude (fallback/complex reasoning)
ANTHROPIC_API_KEY=

# Optional: VAPID keys for Web Push notifications
# Generate with: pip install py-vapid && vapid --gen
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=

# Signing secrets — REQUIRED in production. If unset, the app falls back to
# VAPID_PRIVATE_KEY (a stable secret) and, failing that, refuses to start a
# request rather than signing with a guessable key. Set these explicitly for
# proper key separation. Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SIGNED_URL_SECRET=
AUDIT_CHAIN_SECRET=

# Per-user login passwords (seed users). A user is skipped if its password is
# unset. Format: SEED_PASSWORD_<USERNAME-UPPERCASE>
# SEED_PASSWORD_JALSARRAF=

# Optional: Voice model overrides
# PIPER_VOICE=en_US-amy-medium
# WHISPER_MODEL=base.en
