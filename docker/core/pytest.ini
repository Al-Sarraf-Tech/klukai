[pytest]
asyncio_mode = auto
markers =
    integration: tests that require a live PG/Redis/Qdrant stack
cache_dir = /tmp/pytest_cache
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
