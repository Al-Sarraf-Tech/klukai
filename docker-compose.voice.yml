# RETIRED: the standalone voice deployment was replaced by the canonical
# dominus-nobara AI stack. This file intentionally defines no services, so an
# old `docker compose -f docker-compose.voice.yml up` command cannot create a
# second companion-voice container or compete for Tailscale port 8301.
#
# Canonical Compose file:
#   ops/dominus-nobara/compose.yaml
# Canonical service:
#   companion-voice
# Canonical user unit:
#   dominus-ai-stack.service
# Full procedure:
#   ops/dominus-nobara/RUNBOOK.md

name: klukai-voice-retired

x-retired-deployment:
  host: dominus-nobara
  compose_file: ops/dominus-nobara/compose.yaml
  service: companion-voice
  systemd_user_unit: dominus-ai-stack.service

services: {}
